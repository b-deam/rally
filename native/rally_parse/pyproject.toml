[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "rally_parse"
description = "Prototype native parser for esrally"
requires-python = ">=3.10"
dynamic = ["version"]

[tool.maturin]
module-name = "rally_parse"
